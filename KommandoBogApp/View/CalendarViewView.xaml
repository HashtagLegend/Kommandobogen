<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KommandoBogApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:KommandoBogApp.ViewModel"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    
    x:Class="KommandoBogApp.View.CalendarViewView"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:ActivityViewModel/>
    </Page.DataContext>

    <!--<Page.Resources>
        <ResourceDictionary>
            <Style x:Key="CalendarViewItemDates"  TargetType="CalendarViewDayItem" >
                <Setter Property="MinWidth" Value="40"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="Margin" Value="1"/>
                <Setter Property="Padding" Value="0, 0, 0, 4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CalendarViewDayItem"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Page.Resources>-->


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <CalendarView  Name="CalendarView1" HorizontalAlignment="Left" Margin="667,0,0,0" VerticalAlignment="Top" SelectionMode="Multiple" SelectedDatesChanged="CalendarView_OnSelectedDatesChanged" Height="806" Width="1253" CalendarViewDayItemChanging="CalendarView_CalendarViewDayItemChanging" FirstDayOfWeek="Monday" HorizontalDayItemAlignment="Left" VerticalDayItemAlignment="Top" AutomationProperties.AccessibilityView="Content" />
        
            <ListView Name="ListView1" ItemsSource="{Binding ActivityList.ActivityList}" Margin="0,0,1258,520">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Grid BorderThickness="2" BorderBrush="Black">
                            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Width="166" Margin="0,0,0,0" Text="{Binding Navn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Width="166" Margin="166,0,0,0" Text="{Binding Kommentar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Width="166" Margin="332,0,0,0" Text="{Binding Dates, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Create Activity" HorizontalAlignment="Left" Margin="10,774,0,0" VerticalAlignment="Top" >
            <interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="Click">
                    <Core:CallMethodAction TargetObject="{Binding Mode=OneWay}" MethodName="CreateActivity"/>
                </Core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Create Activity" HorizontalAlignment="Left" Margin="150,774,0,0" VerticalAlignment="Top" >
            <interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="Click">
                    <Core:NavigateToPageAction TargetPage="KommandoBogApp.View.CreateActivityView"/>
                </Core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <TimePicker HorizontalAlignment="Left" Margin="10,621,0,0" VerticalAlignment="Top" Time="{Binding Time, Mode=TwoWay}"/>

    </Grid>
</Page>
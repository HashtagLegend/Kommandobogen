<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KommandoBogApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:KommandoBogApp.ViewModel"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="KommandoBogApp.View.Login"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.DataContext>
            <ViewModel:UserViewModel/>
        </Grid.DataContext>
        <AppBarButton x:Name="LoginButton" HorizontalAlignment="Left" Height="120" Icon="Permissions" Label="Login" Margin="792,749,0,0" VerticalAlignment="Top" Width="383">
            <Interactivity:Interaction.Behaviors>
            </Interactivity:Interaction.Behaviors>
        </AppBarButton>
        <TextBox HorizontalAlignment="Left" Height="117" Margin="684,404,0,0" TextWrapping="Wrap" Text="{Binding LoginString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="568" PlaceholderText="{Binding LoginString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</Page>
